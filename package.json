{
  "name": "vercel-nodejs-app",
  "version": "1.0.0",
  "description": "A Node.js app deployed to Vercel",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Building the project...' && exit 0"
  },
  "dependencies": {
    "express": "^4.17.1",
    "dotenv": "^16.0.0"
    "formidable": "^2.0.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.x"
  }
}
